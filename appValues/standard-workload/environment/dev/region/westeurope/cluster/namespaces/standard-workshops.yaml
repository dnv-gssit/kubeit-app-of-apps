---
defaultRouting:
  kubeitDefaultHosts:
    hostsPrefix: "www-wo"      # Application will be available under: https://${hostsPrefix}-${tenantName}.${env}.kubeit.dnv.com/, instead of https://${app}-${tenantName}.${env}.kubeit.dnv.com/

env:
  KUBERNETES_NAMESPACE: standard-workshops
  MESSAGE: "Hello on standard-workshops namespace!"
