---
controllerManager:
  leaderElection:
    enabled: false
  replicaCount: 1

chaosDaemon:
  runtime: containerd
  socketPath: /run/containerd/containerd.sock

dashboard:
  service:
    type: ClusterIP
  
  # if to enable auth
  securityMode: false
